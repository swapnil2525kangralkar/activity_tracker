import 'package:flutter/material.dart';

class ActivityListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("activity_list_page"),
    );
  }
}
